<%= form_with(model: user_accounts_group, local: true) do |form| %>
  <% if user_accounts_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_accounts_group.errors.count, "error") %> prohibited this user_accounts_group from being saved:</h2>

      <ul>
      <% user_accounts_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :account_id %>
    <%= form.number_field :account_id, id: :user_accounts_group_account_id %>
  </div>

  <div class="field">
    <%= form.label :group_id %>
    <%= form.number_field :group_id, id: :user_accounts_group_group_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
